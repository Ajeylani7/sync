<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sync</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
  <%= stylesheet_link_tag 'forms', media: 'all', 'data-turbo-track': 'reload' %>


</head>
<body>
  <nav class="nav cardo-bold">
  
    <div class="nav__logo">
      <%= image_tag "logo.png", alt: "Logo" %>
    </div>
    <div class="nav__profile">
      <%= image_tag "bunny.png", class: "nav__avatar", alt: "Avatar" %>
    </div>
<div class="nav__links">
  <% if user_signed_in? %><%= link_to 'My Posts', posts_path, class: "nav__posts" %>
<%= button_to 'Sign Out', destroy_user_session_path, method: :delete,  class: "nav__sign" %>



    
  <% else %>
    <%= link_to 'Login', new_user_session_path, class: "nav__login" %>
  <% end %>
</div>

  </nav>

<%= yield %>
<% if !user_signed_in? || (controller_name == 'home' && action_name == 'index') %>
  <section class="hero">
    <div class="hero__content open-sans">
      <h1 class="hero__title">Sync</h1>
      <p class="hero__subtitle">Discover amazing content and connect with others.</p>
    </div>
    <div class="posts">
      <div class="post cardo-regular">
        <h2 class="post__title">Adam </h2>
        <p class="post__content">Hey it's Adam, it's been awhile guys!</p>
      </div>
      <div class="post cardo-regular">
        <h2 class="post__title">Ronald </h2>
        <p class="post__content">That Pizza from 5th Street was Amazing!</p>
      </div>
      <div class="post cardo-regular">
        <h2 class="post__title">Bob </h2>
        <p class="post__content">Quite the weather we're having lately!</p>
      </div>
    </div>
  </section>

<% end %>

  <footer class="footer cardo-bold">
    <div class="footer__content">©Blogger</div>
  </footer>
</body>
</html>
